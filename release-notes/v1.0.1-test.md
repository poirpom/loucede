# loucedé 1.0.1-test

Test end-to-end de la chaîne de release Sparkle.

Cette version n'est pas une vraie release — elle servira uniquement
à valider que le script `release.sh` fonctionne de bout en bout.

Elle sera supprimée à la fin du test.